<% if hospital_signed_in? %>
<%= link_to 'ログアウト', destroy_hospital_session_path, method: :delete %>
<% else %>
<%= link_to "ログイン", new_hospital_session_path %>
<% end %>
<%= form_with(model: @medical_department , local: true ) do |form| %>
  <% if @medical_department.errors.any? %>
    <div id="error_explanation">
      <h2><%= @medical_department.errors.count %>件のエラーがあります。</h2>
      <ul>

      <% @medical_department.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


 <div>

    <%= select_tag :wait_time, options_for_select([["5分", 5],["10分", 10],["15分", 15]]) %>

    <%
=begin%>
 <%= form.number_field :wait_time %> 
<%
=end%>
  </div>
  <div>
    <%= form.label :"診療科" %>
    <%= form.text_field :name %>
  </div> 

 <%
=begin%>
 <%= form.select :wait_time, [["5", 5],[]]%>  
<%
=end%>

  <%= form.submit %>
<% end %>