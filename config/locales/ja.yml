# config/locales/ja.yml
ja:
  activerecord:
    attributes:
      medical_department:
        name: 病院名
    errors:
      messages:
        taken: はすでに存在します
  errors:
    format: ! '%{attribute}%{message}'
